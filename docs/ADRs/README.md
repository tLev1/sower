# ADRs — Architecture Decision Records

One short file per genuinely debatable decision. Format: Context / Decision / Consequences.
If a decision is obvious or easily reversible, don't write an ADR — just make it in code.
